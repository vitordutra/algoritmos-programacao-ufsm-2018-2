#include <stdio.h>
#include <stdlib.h>

#define n 5
/*
17) Definir uma estrutura para armazenar uma coleção de CDs. Os campos (membros) dessa
estrutura devem conter: nome do CD, Nome da banda (ou cantor/cantora) e gênero da música 
(R-rock, P-popular, C-clássica e O-outras). Desenvolver um algoritmo que permita ao usuário
escolher um gênero de música e visualizar na tela todos os CDs do gênero escolhido
armazenados na estrutura. O tamanho da estrutura poderá ser definido pelo programador
*/
typedef struct colecao{
	char nome_cd[20];
	char nome_banda[20];
	char genero;
}col;
int main(int argc, char *argv[]) {
	
	return 0;
}
